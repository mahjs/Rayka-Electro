import * as dns from './dns';
import * as auth from './auth';
import * as plans from './plans';
import * as profile from './profile';
import * as downloads from './downloads';

const api = { dns, auth, profile, downloads, plans };

export default api;
